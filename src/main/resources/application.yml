spring:
  application:
    name: ai-passage-creator
  profiles:
    active: local
  # ? ???? Session
  session:
    redis:
      # ??????JSON
      serializer: json
      flush-mode: on-save
    # session 30 ???
    timeout: 2592000
  # MySQL ???????????
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ai_passage_creator
    username: root
    password: 114514
  # Redis ????????
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      password:

mybatis-flex:
  configuration:
    map-underscore-to-camel-case: false

server:
  port: 8341
  servlet:
    context-path: /api
    session:
      cookie:
        max-age: 2592000

# springdoc-openapi ??
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.kanade.aipassage.controller
# 文章智能体配置
article:
  agent:
    orchestrator:
      # 是否启用多智能体编排模式（支持配图并行生成）
      # true: 使用 Spring AI Alibaba 多智能体编排
      # false: 使用原有的 ArticleAgentService
      enabled: true
    # Agent 最大迭代次数
    max-iterations: 10
